/**
 * Label UI Preferiti — stringhe condivise (tooltip / microcopy).
 * Non è un catalogo i18n: solo costante anti-duplicazione.
 */

export const FAVORITES_UI_LABELS = {
  /** Tooltip quando una città è già nei preferiti (card Home / CityHeader). */
  cityWhenFavorite: 'Città preferita',
} as const;
