/** Max viewport width (px) for the mobile-compact layout (side-by-side hero boxes). */
export const MOBILE_COMPACT_MAX_WIDTH_PX = 767;

/** Media query for mobile-compact layout — single source of truth. */
export const MOBILE_COMPACT_QUERY = `(max-width: ${MOBILE_COMPACT_MAX_WIDTH_PX}px)`;
