/**
 * PROXY EXPORT — src/types/shared.ts
 * 
 * Backward compatibility layer per gli import esistenti.
 * Tutte le definizioni reali sono state migrate nella cartella ./shared/
 * 
 * Source of truth reale: ./shared/index.ts
 */

export * from './shared/index';
